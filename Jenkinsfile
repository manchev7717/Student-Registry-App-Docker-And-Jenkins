pipeline {
    agent any
        stages {}
            stage('NPM install') {
                steps {
                   bat 'npm install'
                }
            }
            stage('Run Tests') {
                steps {
                    bat 'npm test'
                }
            }
    
    
}
